#!/bin/bash

rsync -av ephjos.com ephjos.com:~/
rsync -av lua/.init.lua ephjos.com:/var/www/html/.init.lua

ssh -f ephjos.com '\
  [ -f "/var/run/ephjos.com.pid" ] && kill -TERM $(cat /var/run/ephjos.com.pid); \
  mkdir -p /var/www/html; \
  ./ephjos.com -SSS -d -D /var/www/html \
  '
