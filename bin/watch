#!/bin/bash


cd build;
browser-sync start --server --no-open --no-ui --watch --files "../public/**"  &

cd ..;
(while inotifywait --exclude .swp -e modify -r . &> /dev/null; do
  clear;
  ./bin/build
done &)

wait;
